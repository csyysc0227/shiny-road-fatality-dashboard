ui <- page_navbar(
  title = "Kevin's Shiny Dashboard",
  
  nav_panel("Trend by City Size",
            sidebarLayout(
              sidebarPanel(
                checkboxInput("linear1", "Add trend line?", FALSE)
              ),
              mainPanel(
                plotOutput("plot1")
              )
            )
  ),
  
  nav_panel("Distribution by City Size",
            sidebarLayout(
              sidebarPanel(
                checkboxInput("linear2", "Add trend line?", FALSE)
              ),
              mainPanel(
                plotOutput("plot2")
              )
            )
  )
)
